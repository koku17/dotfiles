# Autostart necessary processes (like notifications daemons,status bars,etc.)
# Or execute your favorite apps at launch like this:

exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# goodies
exec-once = sleep .1 ;waybar
exec-once = sleep .5 ; pidof ^swww-daemon || swww-daemon --no-cache
exec-once = sleep 1; swww img -a $(python ~/.scripts/pick.py image) -t fade ; ln -sf "$(cut -d "$(printf "\u00a")" -f 3 .cache/swww/eDP-1)" /tmp/current-wallpaper
exec-once = sleep 1 ; swaync ; sleep .5 ; swaync -t ; sleep .1 ; swaync -t
exec-once = sleep .5 ; hyprsunset -t 4500

# services
exec-once = sleep 1 ; systemctl start --user hyprpolkitagent

# daemon
exec-once = nm-applet & blueman-applet
exec-once = wl-paste --watch cliphist store
exec-once = wl-clip-persist --clipboard regular
exec-once = hypridle
